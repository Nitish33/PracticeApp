import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  containerStyle: {
    flex: 1,
  },

  imageStyle: {
    position: 'absolute',
    width: '100%',
    height: '100%',
  },

  waspStyle: {
    position: 'absolute',
    width: '100%',
    height: '100%',
    left: -50,
    top: -200,
  },
});

export default styles;
