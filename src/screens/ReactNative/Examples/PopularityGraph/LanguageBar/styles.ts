import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  containerStyle: {
    flexDirection: 'row',
    height: 40,
    marginRight: 10,
    marginVertical: 10,
    // backgroundColor: 'red',
  },
});

export default styles;
